version: '2'
services:
  mqtt:
    build: ./mqtt
    expose:
      - "1883"
    pid: "host"
    network_mode: "host"
    privileged: true
  flogo:
    build: ./flogo
    pid: "host"
    network_mode: "host"
    privileged: true